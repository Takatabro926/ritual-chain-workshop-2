# The deployed RitualPredict. Printed by:
#   cd hardhat && npx hardhat run scripts/setup-local-chain.ts --network localhost
NEXT_PUBLIC_PREDICT_ADDRESS=0x

# Only needed to point at something other than the public Ritual RPC.
# NEXT_PUBLIC_RITUAL_RPC_URL=https://rpc.ritualfoundation.org
